# B2 阶段报告：发电机层按钮机器人可操作 + 场景切换一致性

日期：2026-08-31
状态：B2 完成，4/6 按钮全链路可操作；2/6 按钮被**真实场景几何**阻塞（非配置可修，需你决策）；
场景切换一致性（含此前 503 的 cabinet_operation 切换）已在新栈上验证通过。

> 全部改动保持 **UNCOMMITTED**。等你回来决定推送还是回退。

---

## 一、结论一览

| 按钮 | 结果 | 关键证据 |
|---|---|---|
| fr135_button | ✅ 可操作 | peak=0.0063，物理按压确认 trig=True |
| fr2222_button | ✅ 可操作 | peak=0.0072，trig=True |
| fr4332_button | ✅ 可操作 | peak=0.0072，trig=True |
| fr12452_button | ✅ 可操作（本次修复） | peak=0.0063，trig=True |
| fr20422_button | ❌ **物理不可能** | 按钮埋在真实设备块内部（STL 确认） |
| fr25452_button | ❌ **被真实几何阻塞** | 唯一绕行通道对 0.84m 机器人全部不可通行 |

---

## 二、已验证可操作（4/6）

六组按钮逐一做了 `navigate → operate press` 全链路 Gazebo 实测。前 4 组均：
- 导航到按钮正南停靠点成功
- 右臂 prepress 到位
- 三缸工具按压至 ~0.008（press_threshold）
- 按钮 `state` 翻转为 pressed，**Gazebo 实体可见移动**，`physical_outcome_confirmed=True`（物理触发器确认）

松手后按钮复位到 released。

---

## 三、fr12452 修复详情（本次 B2 的关键代码改动）

**问题**：fr12452 报 "Start state in collision" / prepress 规划失败。

**根因**：`scene_controls/generator_plant_scene.yaml` 中 `collision_6` 立柱代理尺寸用了 **0.5m 宽**，而实际支撑立柱只有 ~0.05m 宽（STL 实测）。0.5m 的幻影代理把 fr12452（x=-21.997）到立柱（x≈-22.4）的净空从 0.38m 压到 0.14m，夹爪预按压路径直接撞上代理盒。

**修复**：`collision_6` 由 `{size:[0.5,0.9,2.06], position:[-22.397,4.4115,1.23]}` 收窄为真实立柱 `{size:[0.05,0.9,2.06]}`；同时把 `collision_1/2/4` 与 xacro 物理碰撞盒逐一对齐。

**验证**：修复后 sweep 中 fr12452 nav+operate 全成功（peak=0.0063，物理按压确认）。

---

## 四、fr20422 — 物理不可能（三重独立证据）

按钮位于 (-30.1, 4.8113, 0.5513)。

1. **按钮埋在真实设备块内部**：STL 实测 `collision_4` 西侧设备块是实体（x∈[-34.26,-25.76]，y∈[3.25,5.25]，z 0→2.26，1.9M 顶点）。按钮嵌入该块前端面板 **1.56m 深**——机器人手根本够不到。
2. **即使移除设备块，几何上也不可达**：按钮两侧立柱净空仅 **0.756m**，小于机器人 0.84m 宽度，车只能停在更南 → 按钮距车中心 **1.79m**，超过工具最大可达 ~1.55m。
3. 若强行让车更近，夹爪会撞上按钮面板 —— 无解。

**结论**：无论怎么调配置/地图，fr20422 都无法操作。这不是 Bug，是场景本身把按钮放在了一个不可达的位置。

---

## 五、fr25452 — 被真实几何阻塞（非配置可修）

按钮位于 (-35.01, 4.8113)。按钮本身几何正常、标准 1.4m 停靠距离，但到达它只有两条绕行路：

| 绕行路线 | 走廊宽度 | 0.6m 膨胀后可用 | 是否可通过 |
|---|---|---|---|
| 南走廊（y∈[1.25,3.25]） | 2.0m | 0.8m | ❌ 小于机器人 0.84m |
| 北缺口（y∈[5.25,6.75]） | 1.5m | 0.3m | ❌ 完全不可行 |

实测 nav2 强行规划直线穿西侧设备块 → 楔在 (-32.47, 2.82)（navigation_timeout）。

**附带发现（地图生成 bug）**：`generate_scene_maps.py` 用垂直射线区间分类（floor_threshold 0.3 / clearance_height 0.9），**看不到"开口朝下"的壳体网格的侧壁**。西侧设备块是 STL 实体，但因其是底部开口壳体，被地图标记为 **FREE**——这让 nav2 以为能直穿。若修好地图让设备块显示为 OCC，fr25452 仍需走上述两条不可通行绕行路，**仍然无解**。

**可选的真正解法**（需你批准，涉及改场景）：
- (a) 移除 `collision_2` 幻影碰撞盒（"北侧设备"，STL 实测只有 309 顶点、极稀疏框架，却配了 16×17m 超大实体盒），北缺口即被打通；
- (b) 修地图生成器（或对西侧设备块改盒代理）。

两者都治本但都需要改场景/换地图生成方式 —— 按你"不改模型物理外观"的红线，**我先不动，等你决定**。

---

## 六、场景切换一致性（B2d）— 已验证

新栈上跑完整 round-trip：`generator_plant → electrical_mezzanine → cabinet_operation → generator_plant`：

| 步骤 | 结果 |
|---|---|
| generator → mezzanine | 200 switched |
| mezzanine → cabinet_operation | **200 switched**（此前 503 的用例，新栈不再复现） |
| cabinet_operation → generator | 200 switched，机器人回 (-16, 2) 出生点 |
| 往返后 fr135 再操作 | **op=success，peak=0.0071，trig=True**（物理按压确认） |

**结论**：场景切换后按钮保持可操作，状态不丢、无需重启节点。之前那次 503 是**劣化栈**（reconcile_pending=True、地板被删）的症状，干净栈上不出现。

**一个瞬态观察**：测试第一步 fr135 失败过一次（Cartesian fraction=0.984，按压路径 1.6cm 处被面板挡住）——根因是前一个脚本留下的未完成导航 + 0.59m AMCL 漂移污染了起始位姿，非切换回归；同一操作在干净起始状态下立即成功（自愈）。记录为已知脆弱点：**operate 对 operate 时刻的 AMCL 漂移敏感**，属于既有系统行为，本次未改动。

---

## 七、附带发现（已记录，未改）

1. **地图生成 bug**：垂直射线分类看不到开口朝下壳体的侧壁 → 西侧设备块被标 FREE（见第五节）。**未修**，避免影响已验证的东侧按钮导航。
2. **collision_2 幻影**：xacro 里"北侧设备"配了 16×17m 实体碰撞盒，但 STL 实际只有 309 顶点的稀疏框架 —— **严重超尺寸**。它压着北侧通道，是 fr25452 无解的第二根支柱。**未动**（改它=改场景几何，需你批准）。
3. **场景切换 homing 脆弱性**：切场景时清理世界阶段的家校复位（reset_joint_tolerance 0.12）在极端情况下会超时（503）。新栈上三轮切换均通过，但这是已知的边际点。

---

## 八、需要你决策的事项

1. **fr20422 / fr25452 是否从 `operable_control_ids` 移除？** 我的建议：保留在 controls 目录做状态展示，但标注为"不可操作"。这样 Web 上能看到 6 组按钮，只有 4 组可点。
2. **西侧设备块是否允许我改？** 若你希望 fr25452 也可操作，需要：(a) 移除/缩小 collision_2 幻影 + (b) 修地图生成或对设备块建盒碰撞代理。两者都触及"场景几何"，等你授权。
3. **B3（夹层门）是否按计划推进？** 详见下节。

---

## 九、B3 计划与可行性初判

电气夹层门（5 扇）需要新增 `slider` 直线推/拉类型（interfaces 常量 + 插件 parse_kind + operator 直线 waypoint + cabinet_client + monitor.html，~250 行）。

**可行性初判（需 live 验证）**：
- 门滑轨为 prismatic 轴 (1,0,0)，行程 0→0.3m；
- 门关节 z 在 0.83~1.93m，接近工具可达窗口上沿；
- 夹层地板大量是空隙（`dianqiground` 网格 11 万顶点散布平台），门正南 y=3.5 有自由格、y=4.0 是墙（OCC）；
- 门把手/按钮是 slide 的子关节，破坏现有 revolute 祖先链 → 需要新的直线操作计算。

**兜底（计划已约定）**：若 live 验证侧向推/拉物理操作不可靠（工具侧向接触保持难），B3 交付为"规划可达 + 状态展示但物理不可操作"——夹具可操作性已由 B1/B2 的 4/6 按钮充分证明。

我接下来会先做 B3 可行性实测（配好夹层 fixture → 导航到门区 → 试操作），根据结果决定做全量 slider 还是按兜底降级，并如实报告。
