# 抽屉解锁服务：右手解锁电缸的真实杆端进入右把手解锁逻辑区后，轨道
# 才能解锁。右把手上的 b1p 是固定指示灯，不是按钮，也没有按压行程。
#
# 解锁要求（两者缺一不可，plugin 在 handle_unlock_request 内判定）：
#   - 请求必须指定真实解锁电缸的移动连杆及其杆端局部坐标，杆端世界位置
#     距 unlock_press_point ≤ unlock_distance_threshold；
#   - 模型中的 unlock_motor_joint 实测伸出量位于
#     [retracted+extension_floor, extension_ceiling) 内。
# unlock=false 用于复位/取消时显式恢复锁止；抽屉回到关位并稳定后也会自动
# 恢复锁止。
#
# unlock_press_point 位于抽屉链路局部系；right_robot_grasp_point 位于真实
# 解锁电缸移动连杆局部系。

string control_id
string operation_lease_id
string robot_model
string right_robot_link
geometry_msgs/Point right_robot_grasp_point
geometry_msgs/Point unlock_press_point
bool unlock
---
bool success
string message
float64 distance
bool drawer_unlocked
# 解锁电缸是否确实伸入规定工作区间。
bool pressed
# 解锁电缸真实杆端是否处于解锁逻辑区容差内。
bool right_tool_contact
